<?php
$commands = array(
	'/usr/bin/python ./simple.py',
);

for ($i = 0; $i < count($commands); $i++) {
	echo "<pre>";
	echo "Command: " . $commands[$i] . "\n";
	echo shell_exec($commands[$i]);
	echo "</pre>";
}
?>