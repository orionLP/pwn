import { ClassMap } from "@hotwired/stimulus/dist/types/core/class_map";
import { DataMap } from "@hotwired/stimulus/dist/types/core/data_map";
import { Guide } from "@hotwired/stimulus/dist/types/core/guide";
import { Logger } from "@hotwired/stimulus/dist/types/core/logger";
import { Schema } from "@hotwired/stimulus/dist/types/core/schema";
import { TargetSet } from "@hotwired/stimulus/dist/types/core/target_set";
import { OutletSet } from "@hotwired/stimulus/dist/types/core/outlet_set";
export declare class Scope {
    readonly schema: Schema;
    readonly element: Element;
    readonly identifier: string;
    readonly guide: Guide;
    readonly outlets: OutletSet;
    readonly targets: TargetSet;
    readonly classes: ClassMap;
    readonly data: DataMap;
    constructor(schema: Schema, element: Element, identifier: string, logger: Logger);
    findElement(selector: string): Element | undefined;
    findAllElements(selector: string): Element[];
    containsElement: (element: Element) => boolean;
    queryElements(selector: string): Element[];
    private get controllerSelector();
    private get isDocumentScope();
    private get documentScope();
}
