import { Application } from "@hotwired/stimulus/dist/types/core/application";
import { Context } from "@hotwired/stimulus/dist/types/core/context";
import { Definition } from "@hotwired/stimulus/dist/types/core/definition";
import { Module } from "@hotwired/stimulus/dist/types/core/module";
import { Scope } from "@hotwired/stimulus/dist/types/core/scope";
import { ScopeObserverDelegate } from "@hotwired/stimulus/dist/types/core/scope_observer";
export declare class Router implements ScopeObserverDelegate {
    readonly application: Application;
    private scopeObserver;
    private scopesByIdentifier;
    private modulesByIdentifier;
    constructor(application: Application);
    get element(): Element;
    get schema(): import("@hotwired/stimulus/dist/types/core/schema").Schema;
    get logger(): import("@hotwired/stimulus/dist/types/core/logger").Logger;
    get controllerAttribute(): string;
    get modules(): Module[];
    get contexts(): Context[];
    start(): void;
    stop(): void;
    loadDefinition(definition: Definition): void;
    unloadIdentifier(identifier: string): void;
    getContextForElementAndIdentifier(element: Element, identifier: string): Context | undefined;
    handleError(error: Error, message: string, detail: any): void;
    createScopeForElementAndIdentifier(element: Element, identifier: string): Scope;
    scopeConnected(scope: Scope): void;
    scopeDisconnected(scope: Scope): void;
    private connectModule;
    private disconnectModule;
}
