import { Controller, ControllerConstructor } from "@hotwired/stimulus/dist/types/core/controller";
import { Definition } from "@hotwired/stimulus/dist/types/core/definition";
import { Dispatcher } from "@hotwired/stimulus/dist/types/core/dispatcher";
import { ErrorHandler } from "@hotwired/stimulus/dist/types/core/error_handler";
import { Logger } from "@hotwired/stimulus/dist/types/core/logger";
import { Router } from "@hotwired/stimulus/dist/types/core/router";
import { Schema } from "@hotwired/stimulus/dist/types/core/schema";
import { ActionDescriptorFilter, ActionDescriptorFilters } from "@hotwired/stimulus/dist/types/core/action_descriptor";
export declare class Application implements ErrorHandler {
    readonly element: Element;
    readonly schema: Schema;
    readonly dispatcher: Dispatcher;
    readonly router: Router;
    readonly actionDescriptorFilters: ActionDescriptorFilters;
    logger: Logger;
    debug: boolean;
    static start(element?: Element, schema?: Schema): Application;
    constructor(element?: Element, schema?: Schema);
    start(): Promise<void>;
    stop(): void;
    register(identifier: string, controllerConstructor: ControllerConstructor): void;
    registerActionOption(name: string, filter: ActionDescriptorFilter): void;
    load(...definitions: Definition[]): void;
    load(definitions: Definition[]): void;
    unload(...identifiers: string[]): void;
    unload(identifiers: string[]): void;
    get controllers(): Controller[];
    getControllerForElementAndIdentifier(element: Element, identifier: string): Controller | null;
    handleError(error: Error, message: string, detail: object): void;
    logDebugActivity: (identifier: string, functionName: string, detail?: object) => void;
    private logFormattedMessage;
}
